program Fib;

function fib(x:integer):integer;
begin
  if (x = 0 or x = 1) then
    fib:=1
  else
    fib:=fib(x - 2) + fib(x - 1);
end;

begin
    WriteLn('Since this is a lib, this main will not be called. so you will not see this message ;)');
end.