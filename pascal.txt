program Printing;

function Print(j : integer);
begin
    WriteLn('j:',j)
end;

begin
    Print(5);
end.



function fib(x:integer):integer;
begin
  if (x = 0 or x = 1) then
    fib:=1
  else
    fib:=fib(x - 2) + fib(x - 1);
end;

function Print(j : integer);
begin
    WriteLn('j:',j)
end;

begin
    i := 21;
    fib_rslt := fib(i);
    Print(fib_rslt);
end.