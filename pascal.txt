program Printing;

function fib(x:integer):integer;
begin
  if (x = 0 or x = 1) then
    fib:=1
  else
    fib:=fib(x - 2) + fib(x - 1);
end;

function Print(j : integer);
begin
    WriteLn('j:',j)
end;

var a : integer;

a := ReadLn();

repeat
    a := a + 1
until a = 10;

begin
    i := 21;
    fib_rslt := fib(i);
    Print(fib_rslt);
end.